import { RealisateursPipe } from './realisateurs.pipe';

describe('RealisateursPipe', () => {
  it('create an instance', () => {
    const pipe = new RealisateursPipe();
    expect(pipe).toBeTruthy();
  });
});
